{
  "name": "ts-samples",
  "version": "0.0.0",
  "description": "Sample Lyra workflow",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "Apache-2.0",
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/lyraproj/lyra"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "devDependencies": {
    "gts": "^0.9.0",
    "typescript": "~3.1.0"
  },
  "dependencies": {
    "lyra-workflow": "^0.1.2-rc.1"
  }
}
