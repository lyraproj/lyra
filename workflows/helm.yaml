returns: [helm_output]
steps:
  helm:
    parameters:
      name: {value: "wordpress"}
      chart: {value: "stable/wordpress"}
      namespace: {value: null}     
      overrides: {value: ["wordpressUsername=somebodynotyou", "wordpressPassword=Anything", "wordpressBlogName=Da Blog", "externalDatabase.Host=myserver"]}
    returns:
      helm_output: output
    call: helm_go
